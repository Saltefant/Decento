<!DOCTYPE html>
<html>
<head>
    <title>Decento - Ordrer</title>
<% include shared/head.ejs %>
</head>

<!-- HEADER -->
<% include shared/header.ejs %>

<!-- NAVIGATION -->
<% include shared/navigation.ejs %>

<body>
<div class="whiteout">
    <div class="content-wrap">

            <h1>Alle ordrer</h1>
            <a href="/profile">< Tilbage til profil</a><br>
            <%if (message.length > 0) { %>
                <div class="alert"><%= message %></div><br>
            <% } %>
            <p>Samtlige ordrer er listet herunder: (<%= orders.length %> ordrer i alt)</p>

        <% for(var i=0; i < orders.length; i++) {%>
            <div class="news-container <%= orders[i].customerResponse.accepted %>">
                <div class="headline-wrap <%=orders[i].order.ordertype%>">
                    <h3>Ordretype: <%= orders[i].order.ordertype %></h3>
                </div>
                <p>
                    <strong>Ordre ID:</strong> <%= orders[i]._id %><br>
                    <strong>Oprettet:</strong> <%= orders[i].order.dateCreated %><br>
                    <strong>Bestilt af:</strong> <a href="/displayuser/<%= orders[i].order.customerId %>"><%= orders[i].order.customerId %></a><br>
                    <strong>Ønsket dato:</strong> <%= orders[i].order.wantedDate %><br>
                    <strong>Detaljer:</strong> <%= orders[i].order.details %><br>
                    <strong>Status:</strong> <%= orders[i].response.status %><br>
                    <strong>Sat dato:</strong> <%= orders[i].response.actualDate %><br><br>
                    <strong>Kommentarer fra DECENTO:</strong> <%= orders[i].response.comments %><br>
                    <strong>Pris:</strong> <u><%= orders[i].response.price %></u><br>
                    <strong>Kundens accept:</strong> <%= orders[i].customerResponse.accepted %><br>
                    <strong>Download link:</strong> <a href="<%= orders[i].response.downloadLink %>"><%= orders[i].response.downloadLink %></a><br>
                    <br>
                    <a href="/displayorder/<%= orders[i]._id %>">Rediger ordre</a><br>
                    <a href="/deleteorder/<%= orders[i].order.customerId %>/<%= orders[i]._id %>">Slet denne ordre</a>
                </p>
            </div>
        <% } %>
        <a href="/profile">< Tilbage til profil</a><br><br>
    </div>
</div>
<!-- FOOTER -->
<%- include shared/footer.ejs %>
</body>
</html>